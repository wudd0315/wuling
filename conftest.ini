[pytest]
log_format = [%(asctime)s.%(msecs)03d] %(levelname)s %(message)s
log_data_format= %Y-%m-%d %H:%M:%S


python_files =  test*.py


marks =
    p0: case level p0
    p1: case level p1
    memory: memory leak case

filterwarnings =
    ignore
